# Copy this file to `.env` and fill in the value.
#
# The cohort directory is published from the Google Sheet as a CSV:
#   File → Share → Publish to web → choose the sheet → "Comma-separated values (.csv)"
# Paste the resulting URL here. It's read at build time via import.meta.env.SHEET_CSV_URL.
SHEET_CSV_URL=
